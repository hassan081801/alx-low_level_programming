#include <stdio.h>
/**
 * main - a program that prints the alphabet in lowercase.
 *
 * Return: 0 Always
 */
int main(void)
{
	char alph;
	for (alph = 'a' ; alph = 'z' ; alph++)

	{
		putchar(alph);
	}
	puchar('\n');
	return (0);
}
